version: '1.0'
services:
  web:
    build:
      context: ./
      target: dev
    volumes:
      - .:/src
    command: npm run nodemon
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
      DEBUG: nodejs-docker-express:*